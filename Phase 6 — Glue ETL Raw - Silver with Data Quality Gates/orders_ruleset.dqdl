Rules = [
    IsComplete "order_id",
    IsComplete "customer_id",
    IsComplete "status",
    IsUnique "order_id",
    ColumnValues "store_region" in ["North","South","East","West","Central"] with threshold >= 0.99,
    ColumnValues "status" in ["Completed","Returned","Cancelled"] with threshold >= 0.99 
]